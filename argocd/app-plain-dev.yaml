apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: app-plain-dev
  namespace: argocd
spec:
  project: demo
  source:
    repoURL: REPO_URL
    targetRevision: main
    path: envs/dev
  destination:
    server: https://kubernetes.default.svc
    namespace: dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
